# jonathanyin/aime_1983_2023_qwq-32b_traces_32768

889 AIME competition problems (1983-2023), each paired with a QwQ-32B reasoning trace generated at a 32,768-token budget, kept only where the model's parsed final answer matched the official answer.

**jonathanyin/aime_1983_2023_qwq-32b_traces_32768** is a single-contributor Hub dataset built by user `jonathanyin`; the repository card carries no prose description, citation, or license, only the `dataset_info` YAML block that declares its columns and split [1]. It lives at https://huggingface.co/datasets/jonathanyin/aime_1983_2023_qwq-32b_traces_32768 . Its `templated_response` column is a ready-made Qwen ChatML string - a system turn whose text reads "You are Qwen, created by Alibaba Cloud" [1], a user turn (the problem plus a "reason step by step" instruction), and an assistant turn split into `<|im_start|>think` (the reasoning) and `<|im_start|>answer` (the final write-up) sub-turns - built from generating QwQ-32B, the Qwen team's reasoning model [2], on AIME problems spanning 1983 through 2023, with no 2024 or 2025 problems present [1]. **The dataset performs no filtering against evaluation-benchmark overlap: AIME problem sets, including years within 1983-2023, are commonly used as math-reasoning evaluation benchmarks, and this release does not exclude or flag any problem for that reason [3].**

**Use it for**: reasoning-trace SFT - the SFT method card - either by consuming `templated_response` as-is (its ChatML template already ends each example after `<|im_end|>`) or by re-templating `Question`, `Reasoning`, and `Solution Attempt` yourself. Every row already has a matching final answer, so this is a correctness-filtered trace set, not a labeled correct/incorrect set. Before scoring or reporting eval numbers, manually exclude any problem ID that also appears in your evaluation benchmark, since none are removed here.

**Licence**: not stated - the repository's `cardData` and tags carry no `license` key at all, and the card body has no license text [1][4]. Ungated, public repository [4].

**Shape**: 889 rows, one config (`default`), one split (`train`); 12 columns [1][5][6].

**Hold out**: no split is provided to hold out from within this file - all 889 rows are `train`. The risk to hold out is external: any AIME problem from 1983-2023 that also sits in an evaluation set you plan to score against, since this release keeps the full 1983-2023 span with no year or ID excluded for that reason [3]. A companion same-author dataset of the underlying problem pool, `jonathanyin/AIME_1983_2023`, lists 919 problems for the same 1983-2023 span [7]; the 30-problem gap (919-889) is not represented here at all, not as `Correct: false` rows - this file drops any problem QwQ-32B did not solve within budget rather than keeping a wrong-answer trace for it, a fact read from the parquet file itself (below).

**Origin**: built by Hub user `jonathanyin`; the reasoning and solution-attempt text is generated by QwQ-32B [1][2], and the correctness label matches the pattern that `Answer Attempt` equals the official `Answer` for 888 of the 889 rows, read from the full downloaded file (see Quality). Hub API at the check date: 299 downloads (885 all-time), 0 likes [4].

**Trained-on-by**: the same Hub account hosts model repositories `qwen2.5-32b_qwq-32b_traces_32768`, `qwen2.5-7b_qwq-32b_traces_32768`, and `qwen2.5-1.5b_qwq-32b_traces_32768`, each an SFT fine-tune of a Qwen2.5-Instruct base via TRL, whose names share this dataset's repository-name suffix `qwq-32b_traces_32768` (the model names add a `qwen2.5-<size>` prefix in place of `aime_1983_2023`) [8][9]. None of their model cards names this dataset in front matter or text, so the match is by name suffix and account only, not a stated citation [8][9]. No other adoption was found.

**Introduced by**: no paper - the dataset card [1], which is metadata-only and names no accompanying blog or write-up.

## Shape

Rows and columns, read from the datasets-server endpoints at the check date [5][6]:

| split | rows |
| --- | --- |
| `train` | 889 |

| column | dtype |
| --- | --- |
| `ID` | string |
| `Year` | int64 |
| `Problem Number` | int64 |
| `Question` | string |
| `Answer` | string |
| `Part` | string |
| `Reasoning` | string |
| `Solution Attempt` | string |
| `Answer Attempt` | float64 |
| `Correct` | bool |
| `templated_response` | string |
| `Token Count` | int64 |

Sizes from the datasets-server `/size` endpoint [5]: 23,557,529 bytes as the served parquet file, 52,701,250 bytes decoded in memory. `Token Count` (read from every one of the 889 rows in the downloaded parquet file) ranges from 2,055 to 32,449, mean 10,609, close to but not exceeding the 32,768-token generation budget named in the repository [1]. `Year` spans 1983 to 2023 across all 889 rows, with 218 rows carrying no `Part` value (the single-round AIME format used through 1999) and 336/335 rows split `I`/`II` (the two-round format used from 2000 on) [1].

## Quality

- Every row's `Correct` value is `True`: read across all 889 rows in the downloaded parquet file, none is `False` [1]. This means the file is pre-filtered to solved problems only - unsolved attempts are absent as rows, not present with a `False` flag - consistent with the 919-vs-889 gap against the companion problem-pool dataset noted above [7].
- `Answer Attempt` (QwQ-32B's parsed numeric answer) equals the official `Answer` for 888 of the 889 rows when compared as numbers; the one exception is `2022-II-8`, whose `Answer` field is stored as the text "080 or 081 (both were accepted)" against an `Answer Attempt` of 80.0, still marked `Correct` - both counts read from all 889 rows of the downloaded parquet file [1].
- `ID` is unique across all 889 rows (no duplicate problem IDs), read from the same full-file check [1].
- No source states an annotator-agreement figure, a measured error rate on the `Reasoning` text, or any known-complaints list; none is invented here.

## Load it

```python
import datasets

REV = "2432cb7bea3a4f03aa037434091a5e950ea4df61"  # main at the check date
ds = datasets.load_dataset(
    "jonathanyin/aime_1983_2023_qwq-32b_traces_32768", revision=REV, split="train"
)  # 889 rows
```

**Trap**: there is no `test` or holdout split to load separately - `split="train"` is the entire file, and any evaluation-overlap exclusion (see Hold out) has to be done by the caller against `ID`/`Year`/`Problem Number`, since the dataset itself performs none [1].

## Neighbors

All read live at the check date from the same `jonathanyin` account; row counts are unpinned since the datasets-server `/size` endpoint takes no revision parameter [10].

- `jonathanyin/aime_1983_2023_qwq-32b_traces` - byte-identical to this dataset: its `data/train-00000-of-00001.parquet` has the same LFS content hash and file size as this repository's, confirmed by comparing the Hub tree API for both repositories [11]. Same 889 rows, same columns; the only difference is the missing `_32768` suffix in the repository name. Prefer this dataset (the `_32768`-suffixed one) since its name states the generation budget explicitly.
- `jonathanyin/aime_1983_2023_qwq-32b_traces_16384` - the same problem pool and model at half the token budget (16,384): 820 rows, a smaller correctness-filtered set than this dataset's 889 [10].
- `jonathanyin/aime_1983_2023_qwq-32b_fcs_traces` - all 919 problems from the pool (no correctness filtering drops rows), with added `First Correct Solution`, `Reasoning Token Count`, and `First Correct Solution Token Count` columns, implying multiple sampled attempts per problem rather than this dataset's single 32,768-budget attempt [10][12].
- `jonathanyin/aime_1983_2023_deepseek-r1_traces_32768` - the same problem pool and 32,768-token budget with DeepSeek-R1 instead of QwQ-32B as the generating model: 877 rows [10].
- `jonathanyin/aime_1983_2023_grok-3-mini-high_traces_32768` - the same problem pool and budget with Grok-3-mini-high as the generating model: 866 rows [10].
- `jonathanyin/AIME_1983_2023` - the underlying problem pool with no traces: `ID`, `Year`, `Problem Number`, `Question`, `Answer`, `Part` only, 919 rows, the full span this dataset's 889 rows are filtered down from [7].

## A row

One served shape (config `default`, split `train`). Row 0, read from the downloaded parquet file, with `Reasoning`, `Solution Attempt`, and `templated_response` truncated [1]:

```json
{
  "ID": "1983-1",
  "Year": 1983,
  "Problem Number": 1,
  "Question": "Let $x$ , $y$ and $z$ all exceed $1$ and let $w$ be a positive number such that $\\log_xw=24$ , $\\log_y w = 40$ and $\\log_{xyz}w=12$ . Find $\\log_zw$ .",
  "Answer": "60",
  "Part": null,
  "Reasoning": "Okay, so I need to find the value of log base z of w, given some other logarithmic equations. Let me see. All variables x, y, z are greater than 1, and w is a positive number. The given equations are log_x(w) = 24, log_y(w) = 40, and log_{xyz}(w) = 12. Hmm, I need to find log_z(w). [...]",
  "Solution Attempt": "Given the equations \\(\\log_x w = 24\\), \\(\\log_y w = 40\\), and \\(\\log_{xyz} w = 12\\), we need to find \\(\\log_z w\\). [...] Thus, the value of \\(\\log_z w\\) is \\(\\boxed{60}\\).",
  "Answer Attempt": 60.0,
  "Correct": true,
  "templated_response": "<|im_start|>system\nYou are Qwen, created by Alibaba Cloud. You are a helpful assistant.<|im_end|>\n<|im_start|>user\nLet $x$ , $y$ and $z$ all exceed $1$ and let $w$ be a positive number such that $\\log_xw=24$ , $\\log_y w = 40$ and $\\log_{xyz}w=12$ . Find $\\log_zw$ .\n\nPlease reason step by step, and put your final answer within \\boxed{}.<|im_end|>\n<|im_start|>assistant\n<|im_start|>think\nOkay, so I need to find the value of log base z of w [...] <|im_start|>answer\nGiven the equations \\(\\log_x w = 24\\) [...] Thus, the value of \\(\\log_z w\\) is \\(\\boxed{60}\\).<|im_end|>\n",
  "Token Count": 3691
}
```

`Part` is `null`/`NaN` for pre-2000 problems (single-round AIME); post-2000 rows carry `"I"` or `"II"` instead [1].

## Where it came from

Built by Hub user `jonathanyin`, whose account also hosts a family of same-shape AIME trace datasets across other generating models (DeepSeek-R1, Grok-3-mini-high) and token budgets, and SFT fine-tunes of Qwen2.5 models whose repository names carry this dataset's `qwq-32b_traces_32768` suffix [8][9][10]. The problem pool is the AIME competition archive from 1983 through 2023, matching the same account's separate `AIME_1983_2023` problem-only dataset at 919 problems [7]. The reasoning and solution text in `Reasoning`/`Solution Attempt`/`templated_response` is generated by QwQ-32B, whose own system-prompt text in `templated_response` says "created by Alibaba Cloud" [1], at a stated 32,768-token generation budget (named in the repository) [1]; no source states the exact harness or sampling settings used to run QwQ-32B, and none is invented here.

## Sources

Fetched 2026-08-11; Hub repositories are mutable, which is why Load it pins the revision.

[1] Repository card (README) YAML front matter for jonathanyin/aime_1983_2023_qwq-32b_traces_32768. https://huggingface.co/datasets/jonathanyin/aime_1983_2023_qwq-32b_traces_32768/raw/main/README.md - the only card content; no prose. Plus the parquet file at `data/train-00000-of-00001.parquet`, downloaded and read in full (all 889 rows) for the column, `Year`, `Part`, `Token Count`, `Correct`, `Answer`/`Answer Attempt`, and `ID`-uniqueness facts above. Fetched 2026-08-11.

[2] QwQ-32B model card. https://huggingface.co/Qwen/QwQ-32B/raw/main/README.md - describes QwQ as "the reasoning model of the Qwen series" and credits the "Qwen Team" in its citation; it does not itself say "Alibaba Cloud" (that phrase appears only in [1]'s own system-prompt text). Fetched 2026-08-11.

[3] Assessment based on [1] and [7]: the repository excludes 2024/2025 AIME problems (per `Year` range read from all 889 rows) but performs no exclusion of any 1983-2023 problem for evaluation-benchmark overlap; no source states such filtering was done.

[4] Hugging Face Hub API record. https://huggingface.co/api/datasets/jonathanyin/aime_1983_2023_qwq-32b_traces_32768?full=true - `sha`, `gated`, `private`, `downloads`, `likes`, tags (no `license:` tag present). `downloadsAllTime` read via the same endpoint's `expand[]=downloadsAllTime` variant. Fetched 2026-08-11.

[5] datasets-server size endpoint. https://datasets-server.huggingface.co/size?dataset=jonathanyin%2Faime_1983_2023_qwq-32b_traces_32768 Fetched 2026-08-11.

[6] datasets-server info endpoint. https://datasets-server.huggingface.co/info?dataset=jonathanyin%2Faime_1983_2023_qwq-32b_traces_32768 Fetched 2026-08-11.

[7] jonathanyin/AIME_1983_2023 dataset card and Hub API. https://huggingface.co/datasets/jonathanyin/AIME_1983_2023/raw/main/README.md and https://huggingface.co/api/datasets/jonathanyin/AIME_1983_2023?full=true - 919-row problem-only companion dataset, same 1983-2023 span, same account. Fetched 2026-08-11.

[8] Model cards for jonathanyin/qwen2.5-7b_qwq-32b_traces_32768 and jonathanyin/qwen2.5-1.5b_qwq-32b_traces_32768. https://huggingface.co/jonathanyin/qwen2.5-7b_qwq-32b_traces_32768/raw/main/README.md and Hub API `cardData` for both - TRL SFT fine-tunes of Qwen2.5-Instruct bases; neither names this dataset in `cardData` or body text. Fetched 2026-08-11.

[9] Hub API record for jonathanyin/qwen2.5-32b_qwq-32b_traces_32768. https://huggingface.co/api/models/jonathanyin/qwen2.5-32b_qwq-32b_traces_32768?full=true - repository exists under the same account with the same name pattern; its README returned "Entry not found" and its `cardData` is empty. Fetched 2026-08-11.

[10] datasets-server size endpoint, one call per neighbor: jonathanyin/aime_1983_2023_qwq-32b_traces_16384, jonathanyin/aime_1983_2023_qwq-32b_fcs_traces, jonathanyin/aime_1983_2023_deepseek-r1_traces_32768, jonathanyin/aime_1983_2023_grok-3-mini-high_traces_32768, jonathanyin/aime_1983_2023_qwq-32b_traces. https://datasets-server.huggingface.co/size?dataset=<id> - unpinned, live. Fetched 2026-08-11.

[11] Hub tree API for the `data/` directory of both repositories. https://huggingface.co/api/datasets/jonathanyin/aime_1983_2023_qwq-32b_traces/tree/main/data and https://huggingface.co/api/datasets/jonathanyin/aime_1983_2023_qwq-32b_traces_32768/tree/main/data - identical `oid`/LFS hash/size for `train-00000-of-00001.parquet` in both. Fetched 2026-08-11.

[12] jonathanyin/aime_1983_2023_qwq-32b_fcs_traces dataset card. https://huggingface.co/datasets/jonathanyin/aime_1983_2023_qwq-32b_fcs_traces/raw/main/README.md - `dataset_info` YAML naming its `First Correct Solution`, `Reasoning Token Count`, `First Correct Solution Token Count` columns and 919-row count. Fetched 2026-08-11.

## Appendix: screening record

### Screening verdict

Usable as SFT reasoning-trace data, with a caution rather than a hard exclusion: the shortlist's flag notes the `Year` field is verified to run 1983-2023 with no 2024/2025 rows, but AIME archive items are not removed for potential overlap with evaluation benchmarks that also draw on those years [row `flag`]. That matches what the card itself establishes above - no evaluation-overlap filtering is stated or observed in the file - so the action is to hold out any problem ID that coincides with your own eval set before training, not to discard the dataset.

### The screening row

The row's own note: "Historical AIME problems (tail at 2023-II-15, no 2024/2025) with full reasoning traces from QwQ-32B at a 32,768-token budget." Its flag: "rule-risk: aime2025 - AIME archive used as train rows with QwQ traces; `Year` verified 1983-2023, but AIME items are not removed."
