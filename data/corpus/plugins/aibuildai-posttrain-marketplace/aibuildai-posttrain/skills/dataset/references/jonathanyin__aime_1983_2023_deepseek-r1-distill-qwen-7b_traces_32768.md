# jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768

830 correctly-solved AIME problems (1983-2023), each paired with a DeepSeek-R1-Distill-Qwen-7B reasoning trace under a 32,768-token budget, pre-formatted into a single Qwen-style chat string.

**jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768** pairs problems from the builder's own 919-problem AIME archive (1983-2023, no split by exam part before 2000 and Part I/II from 2000 onward) with chain-of-thought traces sampled from DeepSeek-ai's DeepSeek-R1-Distill-Qwen-7B, a 7B model distilled from DeepSeek-R1 [1], then kept only where the model's final answer matched the AIME key [2][3]. The repository's own card carries no prose description beyond its YAML feature/split declaration [2]. **Every one of the 830 served rows has `Correct == True`, confirmed by reading all 830 rows at nine offsets spanning the full split: this release already excludes every row where the 7B teacher's answer was wrong, so it is a rejection-filtered subset (830 of the 919-problem pool, 90.3%), not the full attempt set** [3]. **A companion screening rule flags this dataset "rule-risk: aime2025 - Same AIME archive as train rows with distilled-7B traces; AIME items not removed" [4]; every row's `Year` value found while reading the full split falls in 1983-2023, so no row is literally an AIME 2025 problem, but no explicit decontamination step against a later AIME eval set is documented anywhere in the sources checked here.**

**Use it for**: reasoning-trace SFT on a small (7B), lower-capability teacher's chain of thought - the reasoning-trace SFT method card. The restriction: only correctly-answered traces are present, and the note attached to this dataset in the corpus screening record calls the teacher "a distilled 7B teacher, so the quality ceiling is low" [4]. The `templated_response` column is not a message list; it is a single pre-rendered string using Qwen's `<|im_start|>role...<|im_end|>` markup with the assistant turn split into a `<|im_start|>think` segment (the `Reasoning` column) followed by an `<|im_start|>answer` segment (the `Solution Attempt` column), so a collator built for a plain prompt/completion or chat-message schema needs the `Question`, `Reasoning`, and `Solution Attempt` columns instead, or must parse this string [3].

**Licence**: not stated - the Hub API's `cardData` carries no `license` field and the repository has no licence tag; ungated and public (`"gated": false`, `"private": false`) [5]. The one catch: the AIME problems themselves come from a competition archive whose own copyright status is not addressed by any source checked here.

**Shape**: 830 rows, one split (`train`), one config (`default`), 12 columns [6][7].

**Hold out**: nothing in this specific 830-row release is itself an AIME 2025 problem - every `Year` value read across all 830 served rows falls in 1983-2023 [3] - but the corpus screening record still carries an open `rule-risk` flag on this dataset for the same-named AIME archive, and no source checked here documents an explicit filter against a later AIME eval set; see the appendix for the flag verbatim [4].

**Origin**: built by the Hub user `jonathanyin`; problems are the builder's own AIME 1983-2023 archive, traces are generated by DeepSeek-ai's DeepSeek-R1-Distill-Qwen-7B and kept only when correct [2][3][1]. Hub API at the check date (2026-08-11): 207 downloads, 975 `downloadsAllTime`, 0 likes [5].

**Trained-on-by**: none found - no source checked here (the dataset's own card, its Hub listing, or the teacher model's card) states that any released model trained on this specific dataset [2][5][1].

**Introduced by**: no paper - the dataset card [2].

## Shape

Rows and split (datasets-server `/size`) [6]:

| split | rows |
| --- | --- |
| `train` | 830 |

One config, `default`, 12 columns (datasets-server `/info`, cross-checked against the served rows) [7][3]:

| column | dtype | content |
| --- | --- | --- |
| `ID` | string | e.g. `1983-1`, `2023-II-13` |
| `Year` | int64 | 1983-2023 (full split read) |
| `Problem Number` | int64 | position within the exam |
| `Question` | string | AIME problem text (LaTeX) |
| `Answer` | string | AIME key answer |
| `Part` | string | `None` for pre-2000 rows, `I`/`II` from 2000 on (full split read) |
| `Reasoning` | string | the teacher's chain of thought |
| `Solution Attempt` | string | the teacher's written final solution |
| `Answer Attempt` | string | the teacher's extracted final answer |
| `Correct` | bool | `True` for every one of the 830 rows read |
| `templated_response` | string | pre-rendered Qwen-chat-formatted string (system + user + assistant think/answer turns) |
| `Token Count` | int64 | 1,291-31,618 across the 830 rows read, mean 7,800 |

Byte sizes (datasets-server `/size`) [6]: 14,099,250 bytes as Parquet on disk, 33,538,141 bytes decoded in memory. No source states a tokenizer-specific token-length statistic; the `Token Count` column above is the dataset's own stated per-row figure, read from every served row.

## Quality

- Every one of the 830 served rows carries `Correct == True`, read across the full split (nine offsets, 0 through 800, covering all 830 rows) [3]. The dataset is therefore a rejection-filtered subset of the builder's 919-problem AIME 1983-2023 pool - 830/919 = 90.3% retained - computed from this dataset's declared row count and the companion `jonathanyin/AIME_1983_2023` pool's declared row count [2][8].
- No source states a duplicate-row rate or an annotator/verifier agreement figure; all 830 `ID` values read are distinct, so no duplicate `ID` was found in the full split [3].
- The corpus screening note characterizes DeepSeek-R1-Distill-Qwen-7B as "a distilled 7B teacher, so the quality ceiling is low" [4]; no source states a measured accuracy or trace-quality score beyond the binary `Correct` filter already described.

## Load it

```python
import datasets

REV = "5fbd17ea63959a010207035038241d005f267976"  # main at the check date
train = datasets.load_dataset(
    "jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768",
    revision=REV,
    split="train",
)  # 830 rows, all Correct == True
```

**Trap**: this repository has only one split (`train`) and no held-out split of its own; every row is a correctly-answered trace, so nothing here filters out wrong answers for you at load time - that filtering already happened before the data was published, and the `Correct` column is present but constant across the full split, not a signal to filter on further [3].

## Neighbors

All are the same builder's own family, drawing on the same 919-problem AIME 1983-2023 pool; row counts below were fetched live at the check date [6][8][9]:

- `jonathanyin/AIME_1983_2023` - the underlying problem pool with no traces, 919 rows, 6 columns (`ID`, `Year`, `Problem Number`, `Question`, `Answer`, `Part`) [8].
- `jonathanyin/aime_1983_2023_deepseek-r1_traces_32768` - traces from the full DeepSeek-R1 (not the distilled 7B) at the same 32,768-token budget, 877 rows declared (95.4% of the pool by row count) [9].
- `jonathanyin/aime_1983_2023_qwq-32b_traces_32768` - traces from QwQ-32B at the same token budget, 889 rows declared (96.7% of the pool) [9].
- `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-14b_traces_32768` - the 14B distilled sibling at the same token budget, 740 rows declared (80.5% of the pool); the same schema and, in the ten rows sampled at offset 0, the same `Correct == True` pattern seen throughout this dataset [9][10].
- `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-1.5b_traces_32768` - the 1.5B distilled sibling at the same token budget, 716 rows declared (77.9% of the pool) [9].
- `jonathanyin/aime_1983_2023_grok-3-mini-high_traces_32768` - traces from Grok-3-mini-high at the same token budget, 866 rows declared (94.2% of the pool) [9].
- `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_16384` - the same 7B teacher at a smaller 16,384-token budget, 801 rows declared (87.2% of the pool) [9].

The row counts above are read from each repository's declared/served totals only; whether every row in each sibling is `Correct == True` was not checked here except for the 14B sibling's first ten rows, so "kept correct" is not asserted for the others. Within this family, prefer this 32,768-token, 7B-distilled release specifically when the target model is that size class. No source states which release, if any, this corpus prefers over the others.

## A row

One config, one split, so one row covers the served shape. From `config="default"`, `split="train"`, `row_idx=0` (datasets-server `/first-rows`), with `Reasoning`, `Solution Attempt`, and `templated_response` truncated [3]:

```json
{
  "ID": "1983-1",
  "Year": 1983,
  "Problem Number": 1,
  "Question": "Let $x$ , $y$ and $z$ all exceed $1$ and let $w$ be a positive number such that $\\log_xw=24$ , $\\log_y w = 40$ and $\\log_{xyz}w=12$ . Find $\\log_zw$ .",
  "Answer": "60",
  "Part": null,
  "Reasoning": "Alright, so I have this logarithm problem here, and I need to find log_z w. Let me read it again to make sure I understand all the given information. [...] So, I'm confident that the answer is 60.\n\n**Final Answer**\n\\boxed{60}",
  "Solution Attempt": "Given the problem, we need to find \\(\\log_z w\\). We know the following: [...] Thus, the final answer is:\n\n\\[\n\\boxed{60}\n\\]",
  "Answer Attempt": "60",
  "Correct": true,
  "templated_response": "<|im_start|>system\nYou are Qwen, created by Alibaba Cloud. You are a helpful assistant.<|im_end|>\n<|im_start|>user\nLet $x$ , $y$ and $z$ all exceed $1$ [...] Please reason step by step, and put your final answer within \\boxed{}.<|im_end|>\n<|im_start|>assistant\n<|im_start|>think\nAlright, so I have this logarithm problem here [...] <|im_start|>answer\nGiven the problem, we need to find \\(\\log_z w\\). [...] \\boxed{60}\n\\]<|im_end|>\n",
  "Token Count": 2829
}
```

## Where it came from

Built by the Hub user `jonathanyin`. The problems are drawn from the builder's own companion pool `jonathanyin/AIME_1983_2023` (919 AIME problems, 1983-2023, split into Part I/II from the year 2000 onward), whose own dataset card likewise carries no prose description of how the archive was assembled [8]. The reasoning traces in this repository are sampled from DeepSeek-ai's DeepSeek-R1-Distill-Qwen-7B, a Qwen2-architecture model distilled from DeepSeek-R1's reasoning behavior and documented in the DeepSeek-R1 technical report [1]; the presence of `Reasoning`, `Solution Attempt`, `Answer Attempt`, and `Correct` columns, together with every served row having `Correct == True`, shows the traces were generated and then filtered to keep only those whose extracted answer matched the AIME key, but no source checked here describes the generation or filtering procedure in prose [3].

## Sources

In-text markers [n] refer to this list, numbered by first appearance. Every source was read on the check date, 2026-08-11; that date covers every number, quote, and corpus row above. Hub repositories are mutable (a repo can be force-pushed), which is why Load it pins the revision. datasets-server endpoints take no revision parameter and reflect the current default branch, not the pinned commit.

[1] DeepSeek-ai/DeepSeek-R1-Distill-Qwen-7B model card on the Hugging Face Hub. https://huggingface.co/deepseek-ai/DeepSeek-R1-Distill-Qwen-7B - identifies the model as a Qwen2-architecture distillation of DeepSeek-R1 and links the DeepSeek-R1 technical report (arXiv:2501.12948). Fetched via the Hub API (`/api/models/deepseek-ai/DeepSeek-R1-Distill-Qwen-7B`) 2026-08-11.

[2] jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768 dataset card (README). https://huggingface.co/datasets/jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768 - the README file is entirely YAML frontmatter (features, splits, configs), no prose body. Fetched 2026-08-11.

[3] datasets-server rows endpoint, read across the full split at nine offsets (0, 100, 200, 300, 400, 500, 600, 700, 800; lengths 100 or 30) to cover all 830 rows. https://datasets-server.huggingface.co/rows?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768&config=default&split=train - `Correct`, `Year`, `Part`, `ID`, `Token Count` values and the sampled row. Fetched 2026-08-11.

[4] The corpus screening row for `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768`, supplied with this card's request - its `note` and `flag`, read back in the row's own words in the appendix. Checked 2026-08-11.

[5] Hugging Face Hub API record for jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768. https://huggingface.co/api/datasets/jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768?full=true - licence field absence, gate status, `sha`, `downloads`, `likes`; `downloadsAllTime` read through the same endpoint's `expand[]=downloadsAllTime` variant. Fetched 2026-08-11.

[6] datasets-server size endpoint. https://datasets-server.huggingface.co/size?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768 Fetched 2026-08-11.

[7] datasets-server info endpoint. https://datasets-server.huggingface.co/info?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1-distill-qwen-7b_traces_32768 Fetched 2026-08-11.

[8] jonathanyin/AIME_1983_2023 dataset card and Hub API record. https://huggingface.co/datasets/jonathanyin/AIME_1983_2023 - 919-row, 6-column problem pool with no prose body; row count cross-checked against https://huggingface.co/api/datasets/jonathanyin/AIME_1983_2023?full=true . Fetched 2026-08-11.

[9] datasets-server size endpoint, one call per neighbor, for every neighbor row count above: `jonathanyin/aime_1983_2023_deepseek-r1_traces_32768`, `jonathanyin/aime_1983_2023_qwq-32b_traces_32768`, `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-14b_traces_32768`, `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-1.5b_traces_32768`, `jonathanyin/aime_1983_2023_grok-3-mini-high_traces_32768`, and `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-7b_traces_16384`. https://datasets-server.huggingface.co/size?dataset=<id> - this endpoint takes no revision parameter, so these counts are live, not pinned. Fetched 2026-08-11.

[10] datasets-server first-rows endpoint for the 14B sibling. https://datasets-server.huggingface.co/first-rows?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1-distill-qwen-14b_traces_32768&config=default&split=train - ten rows read at offset 0, same schema, all `Correct == True` in that sample; not read further. Fetched 2026-08-11.

## Appendix: screening record

### Screening verdict

Usable as reasoning-trace SFT data for a small-teacher distillation target, with the quality ceiling and the open decontamination flag both carried forward from the screening record: this release is a rejection-filtered, 830-row subset of correctly-solved AIME problems (1983-2023) with DeepSeek-R1-Distill-Qwen-7B traces, and every row's `Year` falls within 1983-2023 as read from the full split [3], but no source checked here documents a decontamination pass against a later AIME evaluation set, matching the open flag below.

### The screening row

The row's own note [4]: "Same problems with traces from DeepSeek-R1-Distill-Qwen-7B, a distilled 7B teacher, so the quality ceiling is low." Its flag [4]: "rule-risk: aime2025 - Same AIME archive as train rows with distilled-7B traces; AIME items not removed."
