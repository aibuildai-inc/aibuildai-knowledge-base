# jonathanyin/aime_1983_2023_deepseek-r1_traces_32768

877 correct DeepSeek-R1 reasoning traces on AIME problems from 1983-2023, generated under a 32,768-token budget and chat-templated for supervised fine-tuning.

**jonathanyin/aime_1983_2023_deepseek-r1_traces_32768** pairs each problem from the builder's own `jonathanyin/AIME_1983_2023` archive (919 problems) [1] with a reasoning trace and final solution generated by DeepSeek-R1, the reasoning model introduced in "DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning" [2]. The repository's only card content is its YAML metadata block; no prose README documents the generation pipeline [3]. The 877 served rows all have `Correct` set to `True` [4], so the file already excludes the problems on which the recorded attempt did not match the reference answer. It lives at https://huggingface.co/datasets/jonathanyin/aime_1983_2023_deepseek-r1_traces_32768 [5].

**Use it for**: reasoning-trace SFT - each row's `templated_response` is a full chat-formatted transcript (Qwen `<|im_start|>`/`<|im_end|>` turns) with the DeepSeek-R1 reasoning wrapped in `<|im_start|>think ... <|im_start|>answer` tags inside the assistant turn, ready to fine-tune directly on that string. See the SFT method card. No source states a licence, so treat reuse as unresolved rather than as either restricted or unrestricted.

**Licence**: not stated - `cardData.license` is absent and the repository carries no `license:` tag; the repo is ungated and public [6].

**Shape**: 877 rows, one config (`default`), one split (`train`), 12 columns [4][7].

**Hold out**: nothing found - `Year` ranges 1983-2023 with no 2024 or 2025 problems present, so this file does not overlap the AIME 2024/2025 problem sets used as recent-year evaluation benchmarks [4].

**Origin**: built by Hugging Face user jonathanyin; the reasoning and solution text is DeepSeek-R1 model output, and `Correct` is a programmatic match against the archive's own `Answer` field rather than a human label [1][4]. Hub API at the check date: `downloads` 547 (30-day rolling count), `downloadsAllTime` 1,518, `likes` 0 [6][8].

**Trained-on-by**: none found - no source in hand documents a named model or training recipe consuming this specific repository.

**Introduced by**: no paper - the dataset card [3]; the teacher model it distills is documented in the DeepSeek-R1 paper [2].

## Shape

Rows and split, from the datasets-server size endpoint [7]:

| split | rows |
| --- | --- |
| `train` | 877 |

Columns and dtypes, from datasets-server `/info` [9]:

| column | dtype |
| --- | --- |
| `ID` | string |
| `Year` | int64 |
| `Problem Number` | int64 |
| `Question` | string |
| `Answer` | string |
| `Part` | string |
| `Reasoning` | string |
| `Solution Attempt` | string |
| `Answer Attempt` | int64 |
| `Correct` | bool |
| `templated_response` | string |
| `Token Count` | int64 |

Sizes: 17,738,483 bytes downloaded (Parquet), 40,033,647 bytes decoded in memory [7]. `Token Count` is stated per row in the data itself (min 1,527, median 6,746, mean 8,444, max 29,991, over all 877 rows) [10]; no source explains what text this count covers, but its 29,991 maximum sits just under the 32,768-token figure in the repository's name, consistent with a generation budget of that size. `Reasoning` field character length ranges 2,726-85,249 (mean 20,549) and `templated_response` character length ranges 4,295-88,209 (mean 22,677), both over all 877 rows [10].

## Quality

- `Correct` is `True` for all 877 served rows (0 rows `False`), per the datasets-server column statistics over the full split [10]. The upstream archive has 919 problems [1], so at least 42 problems (919 minus 877) are absent from this file; no source states whether they were dropped for being unsolved within the token budget, for a generation error, or for another reason.
- `Part` is null for 220 of 877 rows (25.1%) and otherwise splits `I`/`II` roughly evenly (330/327) [10]; no source explains why a quarter of rows lack a Part label.
- No source states a duplicate-row rate, an annotator-agreement figure, or any measured error rate for the `Reasoning` or `Solution Attempt` text; `Correct` is the only stated quality signal.

## Load it

```python
import datasets

REV = "a533437a2dba25d9b938bff495df80fcf0f14795"  # main at the check date
ds = datasets.load_dataset("jonathanyin/aime_1983_2023_deepseek-r1_traces_32768", revision=REV, split="train")  # 877 rows
```

**Trap**: the file has only one split (`train`) and no `test`/`validation` split, so there is no built-in way to hold out part of this repository for evaluation - any such split has to be carved out manually, and doing so risks removing problems that other AIME-teacher siblings in this same author's collection also cover under a different token budget or model.

## Neighbors

The same author published a family of AIME-teacher repositories sharing this base archive; row counts read live at the check date [11]:

- `jonathanyin/AIME_1983_2023` - the source problem archive this repository draws from, 919 rows, six columns (`ID`, `Year`, `Problem Number`, `Question`, `Answer`, `Part`), no reasoning traces [1].
- `jonathanyin/aime_1983_2023_deepseek-r1_traces_16384` - same DeepSeek-R1 teacher at a shorter, 16,384-token budget name, 850 rows; its card lists `Answer Attempt` as a string column rather than the int64 used here, so the two are not a drop-in row-for-row match despite sharing all 12 column names [12][13].
- `jonathanyin/aime_1983_2023_qwq-32b_traces_32768` - same 32,768-token budget, teacher swapped to QwQ-32B, 889 rows [11].
- `jonathanyin/aime_1983_2023_grok-3-mini-high_traces_32768` - same budget, teacher swapped to Grok-3-mini-high, 866 rows [11].
- `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-14b_traces_32768` - same budget, teacher swapped to a DeepSeek-R1-distilled Qwen-14B, 740 rows [11].
- Further siblings at the 16,384-token budget and other DeepSeek-R1-distilled Qwen sizes (7B, 1.5B) exist under the same author but were not row-counted here; none is a re-release of this exact repository [14].

No source states which of these the author intends as the default for training; this card does not choose one, since the row counts differ across all of them.

## A row

One config, one split, so one row covers the file. From `config="default"`, `split="train"`, `row_idx=0` (datasets-server `/first-rows`) [4], with `Reasoning`, `Solution Attempt`, and `templated_response` truncated:

```json
{
  "ID": "1983-1",
  "Year": 1983,
  "Problem Number": 1,
  "Question": "Let $x$ , $y$ and $z$ all exceed $1$ and let $w$ be a positive number such that $\\log_xw=24$ , $\\log_y w = 40$ and $\\log_{xyz}w=12$ . Find $\\log_zw$ .",
  "Answer": "60",
  "Part": null,
  "Reasoning": "Okay, let's see here. I have this problem with logarithms. It says that x, y, z are all greater than 1, and w is a positive number. [...] Therefore, log_z w = 60. So the answer should be 60.",
  "Solution Attempt": "Given the problem, we need to find \\(\\log_z w\\) [...] Thus, the answer is \\(\\boxed{60}\\).",
  "Answer Attempt": 60,
  "Correct": true,
  "templated_response": "<|im_start|>system\nYou are Qwen, created by Alibaba Cloud. You are a helpful assistant.<|im_end|>\n<|im_start|>user\nLet $x$ , $y$ and $z$ all exceed $1$ [...] Please reason step by step, and put your final answer within \\boxed{}.<|im_end|>\n<|im_start|>assistant\n<|im_start|>think\nOkay, let's see here. [...] <|im_start|>answer\nGiven the problem, we need to find \\(\\log_z w\\) [...] Thus, the answer is \\(\\boxed{60}\\).<|im_end|>",
  "Token Count": 2361
}
```

## Where it came from

The problems come from the author's own `jonathanyin/AIME_1983_2023` archive, which carries only `ID`, `Year`, `Problem Number`, `Question`, `Answer`, and `Part` for 919 AIME problems spanning 1983-2023 [1]. This repository adds, per problem, a `Reasoning` trace and a `Solution Attempt` produced by DeepSeek-R1 [2], an `Answer Attempt` extracted from that solution, a `Correct` flag comparing it to the archive's `Answer`, a Qwen-chat-templated `templated_response` combining the question and the reasoning/answer split under `<|im_start|>think`/`<|im_start|>answer` tags, and a `Token Count`. No source states the generation script, sampling settings, or extraction method used to produce these columns.

## Sources

Fetched 2026-08-11; Hub repositories are mutable, which is why Load it pins the revision.

[1] Hugging Face Hub API record for jonathanyin/AIME_1983_2023. https://huggingface.co/api/datasets/jonathanyin/AIME_1983_2023 - row count, column list, licence absence. Fetched 2026-08-11.

[2] DeepSeek-AI et al., "DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning", 2025. https://arxiv.org/abs/2501.12948 - the teacher model. Current title read from the live abs page. Fetched 2026-08-11.

[3] jonathanyin/aime_1983_2023_deepseek-r1_traces_32768 dataset card (README). https://huggingface.co/datasets/jonathanyin/aime_1983_2023_deepseek-r1_traces_32768/raw/main/README.md - the entire card body is a YAML `dataset_info`/`configs` block, no prose. Fetched 2026-08-11.

[4] datasets-server first-rows endpoint. https://datasets-server.huggingface.co/first-rows?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1_traces_32768&config=default&split=train - sampled row, Year range read across the 10 returned rows plus the statistics endpoint [10] for the full-split min/max. Fetched 2026-08-11.

[5] jonathanyin/aime_1983_2023_deepseek-r1_traces_32768 dataset page. https://huggingface.co/datasets/jonathanyin/aime_1983_2023_deepseek-r1_traces_32768 - the item's Hub page. Fetched 2026-08-11.

[6] Hugging Face Hub API record for jonathanyin/aime_1983_2023_deepseek-r1_traces_32768. https://huggingface.co/api/datasets/jonathanyin/aime_1983_2023_deepseek-r1_traces_32768?full=true - licence absence, gate/private status, `sha`, `downloads`, `likes`, last-modified date. Fetched 2026-08-11.

[7] datasets-server size endpoint. https://datasets-server.huggingface.co/size?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1_traces_32768 Fetched 2026-08-11.

[8] Hugging Face Hub API record, `downloadsAllTime` expansion. https://huggingface.co/api/datasets/jonathanyin/aime_1983_2023_deepseek-r1_traces_32768?expand[]=downloadsAllTime Fetched 2026-08-11.

[9] datasets-server info endpoint. https://datasets-server.huggingface.co/info?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1_traces_32768 Fetched 2026-08-11.

[10] datasets-server statistics endpoint. https://datasets-server.huggingface.co/statistics?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1_traces_32768&config=default&split=train - `Correct`, `Part`, `Year`, `Token Count`, `Reasoning`, and `templated_response` column statistics over the full 877-row split. Fetched 2026-08-11.

[11] datasets-server size endpoint, one call per sibling: `jonathanyin/aime_1983_2023_qwq-32b_traces_32768`, `jonathanyin/aime_1983_2023_grok-3-mini-high_traces_32768`, `jonathanyin/aime_1983_2023_deepseek-r1-distill-qwen-14b_traces_32768`. https://datasets-server.huggingface.co/size?dataset=<id> - live, unpinned counts. Fetched 2026-08-11.

[12] datasets-server size endpoint for jonathanyin/aime_1983_2023_deepseek-r1_traces_16384. https://datasets-server.huggingface.co/size?dataset=jonathanyin%2Faime_1983_2023_deepseek-r1_traces_16384 Fetched 2026-08-11.

[13] jonathanyin/aime_1983_2023_deepseek-r1_traces_16384 dataset card (README). https://huggingface.co/datasets/jonathanyin/aime_1983_2023_deepseek-r1_traces_16384/raw/main/README.md - column list and dtypes, including `Answer Attempt: string`. Fetched 2026-08-11.

[14] Hugging Face Hub API dataset listing for author jonathanyin. https://huggingface.co/api/datasets?author=jonathanyin&limit=100 - full list of sibling repository ids. Fetched 2026-08-11.

## Appendix: screening record

### Screening verdict

Usable as reasoning-trace SFT data, with no eval-set overlap found: the served rows are pre-filtered to `Correct=True` DeepSeek-R1 traces [4][10], and the problem years run 1983-2023 with no 2024/2025 items, so the rule check for AIME 2025 contamination found the archive's own year range already excludes it [4].

### The screening row

The row's own note: "Same 1983-2023 problems with DeepSeek-R1 traces at 32,768 tokens; the strongest teacher in this family." Its flag: "rule-risk: aime2025 - Same AIME archive as train rows; `Year` statistics confirm min 1983, max 2023; AIME items retained."
